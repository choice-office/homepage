# Site URL (sitemap, robots, OG 메타데이터에 사용)
NEXT_PUBLIC_SITE_URL=https://example.com

# Google Analytics (선택 — 값 넣으면 layout.tsx에서 자동 활성화)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# ── 문의 폼 통합 ────────────────────────────────────────────────────────────────
#
# 기본 권장 흐름: Supabase 저장 → Resend 알림 이메일 (둘 다 선택, 동시 사용 가능)
# 설정 방법: SETUP.md 6단계 참고
#
# Step 1: Supabase — contacts 테이블에 저장 (pnpm add @supabase/supabase-js 필요)
# SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJxxxxxxxxxxxx   # 서버 전용 — NEXT_PUBLIC_ 붙이지 않음
#
# Step 2: Resend — 알림 이메일 발송 (pnpm add resend 필요, Verified Domain 등록 필수)
# RESEND_API_KEY=re_xxxxxxxxxxxx
# CONTACT_EMAIL — 문의 알림 수신 주소
# ★ 환경별로 다르게 설정한다: 로컬·development·preview = 개발자 메일 / production = 의뢰인(행정사) 메일
#   (개발 중 테스트 문의가 실제 사무소 메일함으로 가지 않게)
# CONTACT_EMAIL=dev@example.com

# ── 공개 콘텐츠 읽기 (블로그·후기·후기이미지) ────────────────────────────────────
#
# Supabase에서 공개(is_published) 데이터를 읽어온다. anon 키 + RLS로 노출본만 조회.
# 미설정 시 각 lib(blog.ts / reviews.ts / review-images.ts)가 로컬 데이터로 폴백한다.
# 스키마: supabase/migrations/*.sql · 데이터 모델: docs/BLOG.md, docs/REVIEWS.md
# SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxxxxxxxx   # 공개 anon 키 — RLS로 노출본만 읽힘
